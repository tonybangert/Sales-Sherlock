# Vercel function deps. The local sherlock package is installed via the
# trailing `.` so that api/*.py can `from sherlock.* import ...`.
fastapi>=0.115.0
pydantic>=2.0.0
anthropic>=0.40.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
markdown-it-py>=3.0.0
.
